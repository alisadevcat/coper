export const formatRolesOptions = (roles) => {
    console.log(roles, 'roles')
    // return roles.reduce((acc, item) => {
    //     acc.push({ value: item.id, label: item.title });
    // }, []);
};
